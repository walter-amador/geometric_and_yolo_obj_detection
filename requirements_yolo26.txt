# Cross-platform requirements for YOLO26n training and inference.
# Install with: pip install -r requirements_yolo26.txt
#
# Torch is pulled in automatically by ultralytics. If you have an NVIDIA GPU
# and want CUDA support, install torch manually first:
#   https://pytorch.org/get-started/locally/

ultralytics>=8.4
opencv-python>=4.10
openvino>=2024.0
matplotlib>=3.9
numpy>=2.0
